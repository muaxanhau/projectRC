import React from 'react'
import { Wrapper } from './elements'

const Avatar = ({ image, size, tooltip }) => {
  return <Wrapper image={image} size={size} tooltip={tooltip} />
}

export default Avatar
