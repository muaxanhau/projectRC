import React from 'react'
import DefaultValues from './../../../constants/DefaultValues'
import { Wrapper } from './elements'

const Version = () => {
  return <Wrapper>Phiên bản {DefaultValues.APP_VERSION}</Wrapper>
}

export default Version
